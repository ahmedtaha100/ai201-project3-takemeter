# Core data + eval (lightweight — needed for collect/label/baseline/evaluate)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
python-dotenv>=1.0
groq>=1.1.2

# Fine-tuning (heavier — also provided in notebooks/takemeter.ipynb for free Colab T4)
torch>=2.2
transformers>=4.46          # processing_class= arg; works through 5.x
datasets>=2.20
accelerate>=1.1.0           # required by transformers Trainer + PyTorch

# Stretch: deployed interface
gradio>=4.0
